<%= simple_form_for(@feed_entry_image) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :feed_entry %>
    <%= f.input :img_url %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
