$('#my-entries').append('<%= j render @feed_entries %>');
<% if @feed_entries.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @feed_entries %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
