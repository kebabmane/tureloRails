<%= content_for :page_title do %>

 | <%= (@feed.feed_name).downcase %>

<% end %>


<% if @feed_entries.any? %>

   <div class="quizzle-card-container mdl-grid">
			<%= render @feed_entries %>
   </div>
  <div id="infinite-scrolling">
		<%= will_paginate @feed_entries %>
  </div>


<% else %>

 <%= render 'layouts/no_data' %>

<% end %>
