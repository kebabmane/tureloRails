<br><br><br>
<div class="container">
<div class="omb_login">
		<h2 class="text-center">sign up</h2>
		<h3 class="text-center">i mean, if you feel like it</h3><br><br>

<div class="container">
		<div class="omb_login">
			<div class="row omb_row-sm-offset-3">
				<div class="col-md-6">
<%= form_for(resource, as: resource_name, :url => registration_path(resource_name), :html => { role: 'form'}) do |f| %>
	<% if @show_errors && current_user.errors.any? %>
		<div id="error_explanation">
			<% current_user.errors.full_messages.each do |msg| %>
				<%= msg %><br>
			<% end %>
		</div>
	<% end %>
	<div class="form-group">
		<div class="controls">
			<%= f.text_field :email, :autofocus => true, :value => '', class: 'form-control input-lg', placeholder: 'email' %>
		</div>
	</div>
	<div class="form-group">
		<div class="controls">
			<%= f.text_field :nickname, :autofocus => true, :value => '', class: 'form-control input-lg', placeholder: 'nickname' %>
		</div>
	</div>
	<div class="form-group">
		<div class="controls">
			<%= f.password_field :password, :value => '', class: 'form-control input-lg', placeholder: 'password' %>
		</div>
	</div>
	<div class="form-group">
		<div class="controls">
			<%= f.password_field :password_confirmation, :value => '', class: 'form-control input-lg', placeholder: 'password again' %>
		</div>
	</div>
	<div class="actions">
		<%= f.button 'sign up', :class => 'btn btn-lg btn-primary btn-block', data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Signing up..."} %>
	</div>
<% end %>

<br>
<div class="text-center">
	<%= render "devise/shared/links" %>
</div>
</div>
</div>
</div>
</div>