<%= simple_form_for(@category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :category %>
    <%= f.association :feeds %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
